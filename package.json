{
  "name": "cloudflare-icons-extractor",
  "version": "1.0.0",
  "description": "Extract Cloudflare icons for Rust/WASM viewer",
  "scripts": {
    "extract-icons": "node extract-icons.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@cloudflare/component-icon": "*",
    "@playwright/test": "^1.54.1",
    "simple-icons": "*"
  }
}
